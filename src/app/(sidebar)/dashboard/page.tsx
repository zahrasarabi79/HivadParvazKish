import { Button, Card, CardContent, CardHeader, Divider, Stack, Typography } from "@mui/material";
import Image from "next/image";
import React from "react";

const Dashboard = () => {
  return (
    <Stack dir="rtl" sx={{ justifyContent: "center", alignItems: "center", width: "100%", height: "45vh" }}>
      <Typography variant="h6">این صفحه در حال آماده سازی است.</Typography>
    </Stack>
  );
};

export default Dashboard;
